好的！让我为你制定一个详细可行的开发计划。我会将整个项目分解成具体的、可执行的任务。
        
            
toolName: todo_write
            
status: success
          
Todos updated: 30 items
          
好的！我已经创建了任务清单。现在让我为你制定一个**详细可行的开发计划文档**，包含每个阶段的具体步骤、时间安排、技术要点和验收标准。

---

# 📋 凌水市集 Web 前端开发计划

## 📊 项目概览

| 项目信息 | 详情 |
|---------|------|
| **项目名称** | 凌水市集 Web 前端 |
| **技术栈** | Vue 3 + TypeScript + Element Plus + Electron |
| **开发周期** | 26-34 天（约 1 个月） |
| **开发模式** | 迭代开发，每周一个里程碑 |
| **目标** | 可打包安装的桌面应用 |

---

## 🎯 开发阶段总览

```
┌─────────────────────────────────────────────────────────┐
│                    开发阶段时间线                        │
├─────────────────────────────────────────────────────────┤
│  第1周：项目初始化 + 用户模块                            │
│    ├─ Day 1-2: 项目搭建与基础配置                       │
│    └─ Day 3-5: 用户登录与个人中心                       │
│                                                         │
│  第2周：商铺模块                                         │
│    ├─ Day 6-7: 商铺列表与详情页                         │
│    └─ Day 8-9: 地图集成与附近商铺                       │
│                                                         │
│  第3周：博客模块                                         │
│    ├─ Day 10-11: 博客列表与详情                         │
│    └─ Day 12-13: 发布博客与点赞                         │
│                                                         │
│  第4周：优惠券 + 秒杀 + 社交                            │
│    ├─ Day 14-16: 优惠券与秒杀                           │
│    └─ Day 17-18: 关注与 Feed 流                         │
│                                                         │
│  第5周：优化 + 打包                                      │
│    ├─ Day 19-22: 测试与优化                             │
│    └─ Day 23-26: Electron 打包与部署                    │
└─────────────────────────────────────────────────────────┘
```

---

## 📝 详细开发计划

### 阶段 1：项目初始化（Day 1-2）

#### 📌 目标
搭建完整的前端项目框架，配置所有必要的开发工具和依赖。

#### 📋 任务清单

| 任务 | 预计时间 | 优先级 | 验收标准 |
|------|---------|--------|---------|
| 1.1 创建 Vue 3 + TypeScript 项目 | 0.5 天 | 🔴 高 | 项目能正常启动 |
| 1.2 安装 Element Plus | 0.5 天 | 🔴 高 | 能使用 Element 组件 |
| 1.3 配置 Vue Router | 0.5 天 | 🔴 高 | 路由跳转正常 |
| 1.4 配置 Axios | 0.5 天 | 🔴 高 | 能发送 HTTP 请求 |
| 1.5 配置 Pinia | 0.5 天 | 🔴 高 | 状态管理可用 |
| 1.6 创建目录结构 | 0.5 天 | 🟡 中 | 目录结构清晰 |

#### 🔧 详细步骤

##### **步骤 1.1：创建 Vue 3 项目**

```bash
# 创建项目
npm create vite@latest lsmarket-web -- --template vue-ts

# 进入项目目录
cd lsmarket-web

# 安装依赖
npm install
```

**验收标准**：
- ✅ 运行 `npm run dev` 能正常启动
- ✅ 浏览器访问 `http://localhost:5173` 能看到欢迎页面

---

##### **步骤 1.2：安装 Element Plus**

```bash
# 安装 Element Plus
npm install element-plus @element-plus/icons-vue

# 安装自动导入插件
npm install -D unplugin-vue-components unplugin-auto-import
```

**配置 `vite.config.ts`**：
```typescript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import AutoImport from 'unplugin-auto-import/vite'
import Components from 'unplugin-vue-components/vite'
import { ElementPlusResolver } from 'unplugin-vue-components/resolvers'

export default defineConfig({
  plugins: [
    vue(),
    AutoImport({
      resolvers: [ElementPlusResolver()],
    }),
    Components({
      resolvers: [ElementPlusResolver()],
    }),
  ],
})
```

**验收标准**：
- ✅ 能在组件中直接使用 `<el-button>` 等组件
- ✅ 不需要手动导入 Element Plus 组件

---

##### **步骤 1.3：配置 Vue Router**

```bash
npm install vue-router@4
```

**创建 `src/router/index.ts`**：
```typescript
import { createRouter, createWebHistory } from 'vue-router'
import type { RouteRecordRaw } from 'vue-router'

const routes: RouteRecordRaw[] = [
  {
    path: '/',
    name: 'Home',
    component: () => import('@/views/Home.vue'),
    meta: { title: '首页' }
  },
  {
    path: '/login',
    name: 'Login',
    component: () => import('@/views/Login.vue'),
    meta: { title: '登录' }
  },
  {
    path: '/user',
    name: 'User',
    component: () => import('@/views/User.vue'),
    meta: { title: '个人中心', requiresAuth: true }
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

// 路由守卫
router.beforeEach((to, from, next) => {
  // 设置页面标题
  document.title = to.meta.title as string || '凌水市集'
  
  // 检查是否需要登录
  if (to.meta.requiresAuth) {
    const token = localStorage.getItem('token')
    if (!token) {
      next('/login')
      return
    }
  }
  
  next()
})

export default router
```

**验收标准**：
- ✅ 能通过路由跳转页面
- ✅ 未登录访问需要认证的页面会跳转到登录页

---

##### **步骤 1.4：配置 Axios**

```bash
npm install axios
```

**创建 `src/utils/request.ts`**：
```typescript
import axios from 'axios'
import { ElMessage } from 'element-plus'
import router from '@/router'

// 创建 axios 实例
const request = axios.create({
  baseURL: 'http://localhost:8081/api',
  timeout: 10000
})

// 请求拦截器
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers['authorization'] = token
    }
    return config
  },
  error => {
    return Promise.reject(error)
  }
)

// 响应拦截器
request.interceptors.response.use(
  response => {
    const res = response.data
    if (res.success) {
      return res.data
    } else {
      ElMessage.error(res.errorMessage || '请求失败')
      return Promise.reject(new Error(res.errorMessage || '请求失败'))
    }
  },
  error => {
    if (error.response?.status === 401) {
      ElMessage.error('登录已过期，请重新登录')
      localStorage.removeItem('token')
      router.push('/login')
    } else {
      ElMessage.error(error.message || '网络错误')
    }
    return Promise.reject(error)
  }
)

export default request
```

**验收标准**：
- ✅ 请求会自动携带 Token
- ✅ 401 错误会自动跳转登录页
- ✅ 错误信息会自动提示

---

##### **步骤 1.5：配置 Pinia**

```bash
npm install pinia
```

**创建 `src/stores/user.ts`**：
```typescript
import { defineStore } from 'pinia'
import { ref } from 'vue'
import request from '@/utils/request'

interface UserDTO {
  id: number
  nickName: string
  icon: string
}

export const useUserStore = defineStore('user', () => {
  const token = ref<string>(localStorage.getItem('token') || '')
  const userInfo = ref<UserDTO | null>(null)

  // 登录
  async function login(phone: string, code: string) {
    const res = await request.post('/user/login', { phone, code })
    token.value = res.token
    localStorage.setItem('token', res.token)
  }

  // 获取用户信息
  async function getUserInfo() {
    const res = await request.get('/user/me')
    userInfo.value = res
  }

  // 登出
  function logout() {
    token.value = ''
    userInfo.value = null
    localStorage.removeItem('token')
  }

  return {
    token,
    userInfo,
    login,
    getUserInfo,
    logout
  }
})
```

**验收标准**：
- ✅ 能存储和读取用户状态
- ✅ 页面刷新后状态不丢失

---

##### **步骤 1.6：创建目录结构**

```
lsmarket-web/
├── src/
│   ├── api/              # API 接口
│   │   ├── user.ts       # 用户相关接口
│   │   ├── shop.ts       # 商铺相关接口
│   │   ├── blog.ts       # 博客相关接口
│   │   └── voucher.ts    # 优惠券相关接口
│   ├── assets/           # 静态资源
│   │   ├── images/       # 图片
│   │   └── styles/       # 样式
│   ├── components/       # 通用组件
│   │   ├── Header.vue    # 头部组件
│   │   ├── Footer.vue    # 底部组件
│   │   └── Map.vue       # 地图组件
│   ├── router/           # 路由配置
│   │   └── index.ts
│   ├── stores/           # 状态管理
│   │   ├── user.ts       # 用户状态
│   │   └── shop.ts       # 商铺状态
│   ├── types/            # TypeScript 类型定义
│   │   ├── user.ts
│   │   ├── shop.ts
│   │   └── common.ts
│   ├── utils/            # 工具函数
│   │   ├── request.ts    # Axios 封装
│   │   ├── auth.ts       # 认证相关
│   │   └── storage.ts    # 本地存储
│   ├── views/            # 页面组件
│   │   ├── Home.vue      # 首页
│   │   ├── Login.vue     # 登录页
│   │   ├── User.vue      # 个人中心
│   │   ├── Shop.vue      # 商铺列表
│   │   ├── ShopDetail.vue# 商铺详情
│   │   ├── Blog.vue      # 博客列表
│   │   ├── BlogDetail.vue# 博客详情
│   │   ├── Voucher.vue   # 优惠券列表
│   │   └── Seckill.vue   # 秒杀页面
│   ├── App.vue           # 根组件
│   └── main.ts           # 入口文件
├── public/               # 公共资源
├── .env.development      # 开发环境变量
├── .env.production       # 生产环境变量
├── vite.config.ts        # Vite 配置
├── tsconfig.json         # TypeScript 配置
└── package.json          # 项目配置
```

**验收标准**：
- ✅ 目录结构清晰
- ✅ 符合 Vue 3 最佳实践

---

### 阶段 2：用户模块（Day 3-5）

#### 📌 目标
实现用户登录、个人中心、签到等核心用户功能。

#### 📋 任务清单

| 任务 | 预计时间 | 优先级 | 验收标准 |
|------|---------|--------|---------|
| 2.1 创建登录页面 UI | 1 天 | 🔴 高 | UI 美观，响应式 |
| 2.2 实现验证码发送 | 0.5 天 | 🔴 高 | 能发送验证码 |
| 2.3 实现登录逻辑 | 0.5 天 | 🔴 高 | 能成功登录 |
| 2.4 创建个人中心 | 1 天 | 🟡 中 | 显示用户信息 |
| 2.5 实现签到功能 | 0.5 天 | 🟡 中 | 能签到并统计 |

#### 🔧 关键代码示例

##### **登录页面**

```vue
<template>
  <div class="login-container">
    <el-card class="login-card">
      <h2>凌水市集</h2>
      <el-form :model="loginForm" :rules="rules" ref="formRef">
        <el-form-item prop="phone">
          <el-input 
            v-model="loginForm.phone" 
            placeholder="请输入手机号"
            prefix-icon="Phone"
          />
        </el-form-item>
        <el-form-item prop="code">
          <el-input 
            v-model="loginForm.code" 
            placeholder="请输入验证码"
            prefix-icon="Lock"
          >
            <template #append>
              <el-button 
                @click="sendCode" 
                :disabled="countdown > 0"
              >
                {{ countdown > 0 ? `${countdown}s` : '获取验证码' }}
              </el-button>
            </template>
          </el-input>
        </el-form-item>
        <el-form-item>
          <el-button 
            type="primary" 
            @click="handleLogin" 
            :loading="loading"
            style="width: 100%"
          >
            登录
          </el-button>
        </el-form-item>
      </el-form>
    </el-card>
  </div>
</template>

<script setup lang="ts">
import { ref, reactive } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import { useUserStore } from '@/stores/user'
import request from '@/utils/request'

const router = useRouter()
const userStore = useUserStore()

const formRef = ref()
const loading = ref(false)
const countdown = ref(0)

const loginForm = reactive({
  phone: '',
  code: ''
})

const rules = {
  phone: [
    { required: true, message: '请输入手机号', trigger: 'blur' },
    { pattern: /^1[3-9]\d{9}$/, message: '手机号格式不正确', trigger: 'blur' }
  ],
  code: [
    { required: true, message: '请输入验证码', trigger: 'blur' },
    { len: 6, message: '验证码为6位数字', trigger: 'blur' }
  ]
}

// 发送验证码
async function sendCode() {
  try {
    await formRef.value.validateField('phone')
    await request.post('/user/code', { phone: loginForm.phone })
    ElMessage.success('验证码已发送')
    countdown.value = 60
    const timer = setInterval(() => {
      countdown.value--
      if (countdown.value <= 0) {
        clearInterval(timer)
      }
    }, 1000)
  } catch (error) {
    console.error(error)
  }
}

// 登录
async function handleLogin() {
  try {
    await formRef.value.validate()
    loading.value = true
    await userStore.login(loginForm.phone, loginForm.code)
    ElMessage.success('登录成功')
    router.push('/')
  } catch (error) {
    console.error(error)
  } finally {
    loading.value = false
  }
}
</script>

<style scoped>
.login-container {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.login-card {
  width: 400px;
  padding: 20px;
}

h2 {
  text-align: center;
  margin-bottom: 30px;
  color: #333;
}
</style>
```

---

### 阶段 3：商铺模块（Day 6-9）

#### 📌 目标
实现商铺浏览、详情查看、地图展示等功能。

#### 📋 任务清单

| 任务 | 预计时间 | 优先级 | 验收标准 |
|------|---------|--------|---------|
| 3.1 创建商铺列表页 | 1 天 | 🔴 高 | 能展示商铺列表 |
| 3.2 创建商铺详情页 | 1 天 | 🔴 高 | 能查看详情 |
| 3.3 集成高德地图 | 1 天 | 🔴 高 | 地图正常显示 |
| 3.4 实现附近商铺 | 0.5 天 | 🟡 中 | 能按距离排序 |

#### 🔧 关键技术点

##### **高德地图集成**

```bash
npm install @amap/amap-jsapi-loader
```

```typescript
import AMapLoader from '@amap/amap-jsapi-loader'

// 初始化地图
AMapLoader.load({
  key: 'YOUR_AMAP_KEY',
  version: '2.0',
  plugins: ['AMap.Geolocation', 'AMap.PlaceSearch']
}).then((AMap) => {
  const map = new AMap.Map('container', {
    zoom: 15,
    center: [longitude, latitude]
  })
  
  // 获取用户位置
  map.plugin('AMap.Geolocation', () => {
    const geolocation = new AMap.Geolocation()
    geolocation.getCurrentPosition((status, result) => {
      if (status === 'complete') {
        const { lng, lat } = result.position
        // 查询附近商铺
        getNearbyShops(lng, lat)
      }
    })
  })
})
```

---

### 阶段 4：博客模块（Day 10-13）

#### 📌 目标
实现博客浏览、发布、点赞等社交功能。

#### 📋 任务清单

| 任务 | 预计时间 | 优先级 | 验收标准 |
|------|---------|--------|---------|
| 4.1 创建博客列表页 | 1 天 | 🔴 高 | 能展示博客列表 |
| 4.2 创建博客详情页 | 1 天 | 🔴 高 | 能查看详情和评论 |
| 4.3 实现发布博客 | 1 天 | 🟡 中 | 能上传图片和文字 |
| 4.4 实现点赞功能 | 0.5 天 | 🟡 中 | 能点赞/取消点赞 |

---

### 阶段 5：优惠券与秒杀（Day 14-16）

#### 📌 目标
实现优惠券领取和秒杀抢购功能。

#### 📋 任务清单

| 任务 | 预计时间 | 优先级 | 验收标准 |
|------|---------|--------|---------|
| 5.1 创建优惠券列表 | 0.5 天 | 🟡 中 | 能展示优惠券 |
| 5.2 创建秒杀页面 | 1 天 | 🔴 高 | UI 美观，倒计时正常 |
| 5.3 实现秒杀逻辑 | 1 天 | 🔴 高 | 能成功抢购 |

#### 🔧 秒杀关键代码

```vue
<script setup lang="ts">
import { ref, onMounted, onUnmounted } from 'vue'
import { throttle } from 'lodash-es'

const loading = ref(false)
const countdown = ref(0)
const stock = ref(100)

// 倒计时
let timer: number
onMounted(() => {
  timer = setInterval(() => {
    if (countdown.value > 0) {
      countdown.value--
    }
  }, 1000)
})

onUnmounted(() => {
  clearInterval(timer)
})

// 秒杀（防抖处理）
const handleSeckill = throttle(async () => {
  if (loading.value || countdown.value > 0) return
  
  loading.value = true
  try {
    await request.post(`/voucher-order/seckill/${voucherId}`)
    ElMessage.success('秒杀成功！')
    stock.value--
    countdown.value = 5 // 5秒冷却
  } catch (error) {
    ElMessage.error(error.message)
  } finally {
    loading.value = false
  }
}, 1000)
</script>
```

---

### 阶段 6：社交模块（Day 17-18）

#### 📌 目标
实现关注、Feed 流等社交功能。

#### 📋 任务清单

| 任务 | 预计时间 | 优先级 | 验收标准 |
|------|---------|--------|---------|
| 6.1 实现关注功能 | 0.5 天 | 🟡 中 | 能关注/取关 |
| 6.2 实现 Feed 流 | 1 天 | 🟡 中 | 能看到关注用户的博客 |

---

### 阶段 7：优化与测试（Day 19-22）

#### 📌 目标
优化性能，修复 Bug，提升用户体验。

#### 📋 任务清单

| 任务 | 预计时间 | 优先级 | 验收标准 |
|------|---------|--------|---------|
| 7.1 功能测试 | 2 天 | 🔴 高 | 所有功能正常 |
| 7.2 性能优化 | 1 天 | 🟡 中 | 首屏加载 < 3s |
| 7.3 Bug 修复 | 1 天 | 🔴 高 | 无严重 Bug |

#### 🔧 性能优化方案

1. **路由懒加载**：已实现
2. **图片懒加载**：使用 `v-lazy` 指令
3. **组件按需加载**：Element Plus 自动导入
4. **代码分割**：Vite 自动处理
5. **缓存策略**：使用 `keep-alive` 缓存页面

---

### 阶段 8：Electron 打包（Day 23-26）

#### 📌 目标
将 Web 应用打包成桌面应用。

#### 📋 任务清单

| 任务 | 预计时间 | 优先级 | 验收标准 |
|------|---------|--------|---------|
| 8.1 集成 Electron | 1 天 | 🔴 高 | 能在 Electron 中运行 |
| 8.2 配置打包 | 1 天 | 🔴 高 | 能生成安装包 |
| 8.3 测试安装包 | 1 天 | 🔴 高 | 安装包能正常安装运行 |
| 8.4 编写文档 | 0.5 天 | 🟢 低 | 有使用说明 |

#### 🔧 Electron 集成步骤

##### **步骤 1：安装 Electron**

```bash
npm install -D electron electron-builder
npm install -D vite-plugin-electron
```

##### **步骤 2：配置 Vite**

```typescript
// vite.config.ts
import electron from 'vite-plugin-electron'

export default defineConfig({
  plugins: [
    vue(),
    electron([
      {
        entry: 'electron/main.ts',
        onstart(options) {
          options.startup()
        },
      },
    ]),
  ],
})
```

##### **步骤 3：创建主进程**

```typescript
// electron/main.ts
import { app, BrowserWindow } from 'electron'
import path from 'path'

function createWindow() {
  const win = new BrowserWindow({
    width: 1200,
    height: 800,
    webPreferences: {
      nodeIntegration: true,
      contextIsolation: false,
    },
  })

  if (process.env.NODE_ENV === 'development') {
    win.loadURL('http://localhost:5173')
    win.webContents.openDevTools()
  } else {
    win.loadFile(path.join(__dirname, '../dist/index.html'))
  }
}

app.whenReady().then(createWindow)

app.on('window-all-closed', () => {
  if (process.platform !== 'darwin') {
    app.quit()
  }
})
```

##### **步骤 4：配置打包**

```json
// package.json
{
  "build": {
    "appId": "com.lsmarket.app",
    "productName": "凌水市集",
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "dist/**/*",
      "electron/**/*"
    ],
    "win": {
      "target": "nsis",
      "icon": "build/icon.ico"
    }
  }
}
```

##### **步骤 5：打包命令**

```bash
# 构建 Vue 应用
npm run build

# 打包 Electron
npm run electron:build
```

**验收标准**：
- ✅ 生成 `.exe` 安装包
- ✅ 安装包大小 < 150MB
- ✅ 安装后能正常运行

---

## 🎯 里程碑与验收标准

### 第 1 周里程碑（Day 5）

**验收标准**：
- ✅ 项目框架搭建完成
- ✅ 能正常登录和登出
- ✅ 个人中心能显示用户信息
- ✅ 签到功能正常

### 第 2 周里程碑（Day 9）

**验收标准**：
- ✅ 商铺列表能正常展示
- ✅ 商铺详情页能正常查看
- ✅ 地图能正常显示
- ✅ 附近商铺功能正常

### 第 3 周里程碑（Day 13）

**验收标准**：
- ✅ 博客列表能正常展示
- ✅ 博客详情页能正常查看
- ✅ 能发布博客
- ✅ 点赞功能正常

### 第 4 周里程碑（Day 18）

**验收标准**：
- ✅ 优惠券列表能正常展示
- ✅ 秒杀功能正常
- ✅ 关注功能正常
- ✅ Feed 流能正常展示

### 第 5 周里程碑（Day 26）

**验收标准**：
- ✅ 所有功能测试通过
- ✅ 性能优化完成
- ✅ Electron 打包成功
- ✅ 安装包能正常安装运行

---

## ⚠️ 风险控制与应对

| 风险 | 概率 | 影响 | 应对措施 |
|------|------|------|----------|
| 后端 API 不稳定 | 中 | 高 | 使用 Mock 数据先行开发 |
| 高德地图 API 限制 | 低 | 中 | 申请多个 Key 轮换使用 |
| Electron 打包失败 | 中 | 高 | 使用 electron-vite 脚手架 |
| 性能不达标 | 中 | 中 | 提前进行性能测试和优化 |
| 跨平台兼容性问题 | 低 | 中 | 在多个平台充分测试 |

---

## 📚 学习资源

### 官方文档
- **Vue 3**: https://cn.vuejs.org/
- **Element Plus**: https://element-plus.org/zh-CN/
- **Pinia**: https://pinia.vuejs.org/zh/
- **Electron**: https://www.electronjs.org/
- **高德地图**: https://lbs.amap.com/

### 推荐教程
- Vue 3 官方教程（必看）
- Element Plus 组件库文档
- Electron 快速入门

---

## 📊 进度跟踪表

建议使用以下表格跟踪每日进度：

| 日期 | 计划任务 | 实际完成 | 遇到问题 | 解决方案 |
|------|---------|---------|---------|---------|
| Day 1 | 项目初始化 | ✅ | 无 | - |
| Day 2 | 配置路由和状态管理 | ✅ | 无 | - |
| Day 3 | 登录页面 | ✅ | 无 | - |
| ... | ... | ... | ... | ... |

---

**准备好开始了吗？** 如果你想开始实施这个计划，我可以帮你：
1. 创建项目并配置所有依赖
2. 实现第一个功能模块（登录）
3. 解决开发过程中遇到的任何问题

你想从哪里开始？ 🚀